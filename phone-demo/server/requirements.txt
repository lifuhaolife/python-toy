# 服务器配置
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-dotenv>=1.0.0
pydantic>=2.5.0
httpx>=0.26.0
aiofiles>=23.2.0

# TTS
edge-tts>=6.1.0

# 语音识别（可选）
# 方案 1: Sherpa-ONNX（推荐，最快，已安装）
sherpa-onnx>=1.0.0

# 方案 2: FunASR Paraformer（中文优化）
# funasr>=1.0.0
# modelscope>=1.0.0
# torch>=2.0.0
# 安装：conda install -c conda-forge funasr

# 方案 3: Whisper（备用，多语言）
# openai-whisper>=20231117
# faster-whisper>=0.10.0
